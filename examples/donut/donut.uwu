A = 1  B = 1      b = []  z = []    A = A + 0.07  B = B + 0.03    cA = math.cos(A)  sA = math.sin(A)  cB = math.cos(B)  sB = math.sin(B)    k = 0  while k < 1760 {  if k % 80 == 79 {   append(b, "\n")  }   else {   append(b, " ")  }  append(z, 0)  k = k + 1  }    j = 0  while j < 6.28 {  ct = math.cos(j)  st = math.sin(j)  i = 0  while i < 6.28 {  sp = math.sin(i)  cp = math.cos(i)  h = ct + 2   D = 1 / (sp * h * sA + st * cA + 5)   t = sp * h * cA - st * sA     x = int(40 + 30 * D * (cp * h * cB - t * sB))  y = int(12 + 15 * D * (cp * h * sB + t * cB))  o = x + 80 * y  N = (8 *  ((st * sA - sp * ct * cA) * cB -  sp * ct * sA -  st * cA -  cp * ct * sB))    if (y < 22 and y >= 0 and x >= 0 and x < 79 and D > z[int(o)]) {  z[int(o)] = D  if N > 0 {  b[int(o)] = ".,-~:=!*#$@A"[int(N)]  }  else {  b[int(o)] = "."  }  }  i = i + 0.02  }  j = j + 0.07  }  for g in b {  print(g)  }  
